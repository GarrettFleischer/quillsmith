# External design skills

Install when your environment allows (used during UI polish):

```bash
npx impeccable install --providers=cursor --scope=project
npx skills add https://github.com/Leonxlnx/taste-skill --skill "design-taste-frontend" -y
```

Aesthetic direction is already locked in `inspiration/BRIEF.md` (Ink Ledger).
